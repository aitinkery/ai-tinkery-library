[project]
name = "ai-tinkery-library"
version = "0.1.0"
description = "Filterable library of Stanford AI Tinkery activities with an AI tutor panel"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
]
